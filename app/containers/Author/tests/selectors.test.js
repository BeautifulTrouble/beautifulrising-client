// import { fromJS } from 'immutable';
// import { makeSelectAuthorDomain } from '../selectors';

// const selector = makeSelectAuthorDomain();

describe('makeSelectAuthorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
