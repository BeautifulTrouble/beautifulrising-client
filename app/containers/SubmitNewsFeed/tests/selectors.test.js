// import { fromJS } from 'immutable';
// import { makeSelectSubmitNewsFeedDomain } from '../selectors';

// const selector = makeSelectSubmitNewsFeedDomain();

describe('makeSelectSubmitNewsFeedDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
