// import { fromJS } from 'immutable';
// import { makeSelectWhatsHappeningDomain } from '../selectors';

// const selector = makeSelectWhatsHappeningDomain();

describe('makeSelectWhatsHappeningDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
