// import { fromJS } from 'immutable';
// import { makeSelectToolsDomain } from '../selectors';

// const selector = makeSelectToolsDomain();

describe('makeSelectToolsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
