// import { fromJS } from 'immutable';
// import { makeSelectStaticTextHandlerDomain } from '../selectors';

// const selector = makeSelectStaticTextHandlerDomain();

describe('makeSelectStaticTextHandlerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
