// import { fromJS } from 'immutable';
// import { makeSelectSubmitRealWorldExampleDomain } from '../selectors';

// const selector = makeSelectSubmitRealWorldExampleDomain();

describe('makeSelectSubmitRealWorldExampleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
