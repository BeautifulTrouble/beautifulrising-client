// import { fromJS } from 'immutable';
// import { makeSelectContactUsDomain } from '../selectors';

// const selector = makeSelectContactUsDomain();

describe('makeSelectContactUsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
