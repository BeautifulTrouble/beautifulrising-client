// import { fromJS } from 'immutable';
// import { makeSelectNewsFeedDomain } from '../selectors';

// const selector = makeSelectNewsFeedDomain();

describe('makeSelectNewsFeedDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
