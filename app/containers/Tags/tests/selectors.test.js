// import { fromJS } from 'immutable';
// import { makeSelectTagsDomain } from '../selectors';

// const selector = makeSelectTagsDomain();

describe('makeSelectTagsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
