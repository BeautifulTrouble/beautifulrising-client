// import { fromJS } from 'immutable';
// import { makeSelectAskTheContributorDomain } from '../selectors';

// const selector = makeSelectAskTheContributorDomain();

describe('makeSelectAskTheContributorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
