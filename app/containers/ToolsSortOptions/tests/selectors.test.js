// import { fromJS } from 'immutable';
// import { makeSelectToolsSortOptionsDomain } from '../selectors';

// const selector = makeSelectToolsSortOptionsDomain();

describe('makeSelectToolsSortOptionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
