// import { fromJS } from 'immutable';
// import { makeSelectToolPageDomain } from '../selectors';

// const selector = makeSelectToolPageDomain();

describe('makeSelectToolPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
