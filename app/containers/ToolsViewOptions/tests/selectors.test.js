// import { fromJS } from 'immutable';
// import { makeSelectToolsViewOptionsDomain } from '../selectors';

// const selector = makeSelectToolsViewOptionsDomain();

describe('makeSelectToolsViewOptionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
