// import { fromJS } from 'immutable';
// import { makeSelectTranslatableStaticTextDomain } from '../selectors';

// const selector = makeSelectTranslatableStaticTextDomain();

describe('makeSelectTranslatableStaticTextDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
