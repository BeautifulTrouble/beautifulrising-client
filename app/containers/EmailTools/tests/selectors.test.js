// import { fromJS } from 'immutable';
// import { makeSelectEmailToolsDomain } from '../selectors';

// const selector = makeSelectEmailToolsDomain();

describe('makeSelectEmailToolsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
