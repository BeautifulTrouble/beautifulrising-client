// import React from 'react';
// import { shallow } from 'enzyme';

// import ToolTypeSelectedFull from '../index';

describe('<ToolTypeSelectedFull />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
