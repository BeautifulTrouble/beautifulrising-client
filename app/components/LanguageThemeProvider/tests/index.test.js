// import React from 'react';
// import { shallow } from 'enzyme';

// import LanguageThemeProvider from '../index';

describe('<LanguageThemeProvider />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
