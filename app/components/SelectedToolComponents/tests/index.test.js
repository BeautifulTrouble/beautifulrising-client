// import React from 'react';
// import { shallow } from 'enzyme';

// import SelectedToolComponents from '../index';

describe('<SelectedToolComponents />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
